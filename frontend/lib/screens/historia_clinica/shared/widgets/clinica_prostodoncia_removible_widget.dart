import 'package:flutter/material.dart';

class ClinicaProstodonciaRemovibleWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Formulario de Prostodoncia Removible (en desarrollo)',
          style: TextStyle(fontSize: 18)),
    );
  }
}
