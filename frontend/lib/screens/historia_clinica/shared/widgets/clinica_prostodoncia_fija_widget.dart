import 'package:flutter/material.dart';

class ClinicaProstodonciaFijaWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Formulario de Prostodoncia Fija (en desarrollo)',
          style: TextStyle(fontSize: 18)),
    );
  }
}
