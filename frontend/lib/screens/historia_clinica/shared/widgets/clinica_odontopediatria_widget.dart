import 'package:flutter/material.dart';

class ClinicaOdontopediatriaWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Formulario de Odontopediatría (en desarrollo)',
          style: TextStyle(fontSize: 18)),
    );
  }
}
